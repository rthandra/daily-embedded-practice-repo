#include <stdio.h>


#define UART_DR (*(volatile uint32_t*)0x1234)
#define UART_SR (*(volatile uint32_t*)0x40021000
#define UART_STATUS_REG (1<<5)

void send(char c){
    while(!(UART_SR & UART_STATUS_REG)){
        
    }
    UART_DR = (uint32_t)c;
}

