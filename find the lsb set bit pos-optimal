// Online C compiler to run C program online
#include <stdio.h>
#include <math.h>

/*

right shift until n is 0 - brute force
optimal - lsb is the highest power of 2 so log formula works

*/
int main() {
  int n = 1; //0001
  int pos = log2(n) + 1;
   printf("%d", pos);
  
  /*
while(n){
    n >>= 1;
    pos++;
 }
 printf("%d", pos);
  return 0;
*/
}
