#include <stdio.h>
#include <stdint.h>

int main()
{
    int n = 0;
    scanf("%d", &n);
    
    if ( n > 0 && (n & (n-1)) == 0){
        printf("power of 2");
    }
    
    else{
        printf("not power of 2");
    }

    return 0;
}
