//brute force
/*void findBase2(int power){
    int base = 0;
    int multiply = 1;
    
    while(multiply != power){
        multiply *= 2;
        base++;
    }
    printf("base = %d", base);
}

void main(){
    
    findBase2(16);
}*/

//bitwise trick

void findBase2(int power){
    int base = 0;
    
    while(!(power&1)){
        power = power>>1;
        base++;
    }
    printf("base = %d", base);
}

void main(){
    
    findBase2(1);
}

