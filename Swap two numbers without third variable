#include <stdio.h>

/* SWAP TWO NUMBERS */

int main()
{
    int a = 0b0101;
    int b = 0b0100;
    printf(" a = %d, b = %d", a, b);
    
    a = a^b;
    b = a^b;
    a = a^b;
    
    printf(" a = %d, b = %d", a, b);

    return 0;
}
