*
HELlo WorLd -> helLO wORlD

step 1 : have a for loop
step 2 : check if its upper case or lower case
step3: flip them
*/

#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main()
{
    char n[] = "HEllo WoRld";
    int length = strlen(n); //char expects strlen
    int mask = 0x20;
    
    for(int i = 0; i < length; i++){
        if (isalpha (n[i])){
            n[i] ^= mask;
        }

    }
    
    printf("n = %s", n); //printing string

    return 0;
}
