int main() {

  uint_fast64_t n[] = {1,2};
  int size = sizeof(n) / sizeof(n[0]);

  for(int i = 0; i < size; i++){
    for(int j = 0; j < 8; j++){
        uint8_t byte = (n[i] >> (j * 8)) & 0xFF;
        printf("%d\n", byte);
    }
  }
  return 0;
}
